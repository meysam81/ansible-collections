---
- name: Enable and start dnsproxy
  ansible.builtin.systemd:
    name: dnsproxy
    state: started
    enabled: true
    daemon_reload: true

- name: Restart dnsproxy
  ansible.builtin.systemd:
    name: dnsproxy
    state: restarted
    daemon_reload: true
    enabled: true
