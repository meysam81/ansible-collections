---
galaxy_info:
  author: Meysam Azad
  role_name: dovecot
  description: Install and configure Dovecot 2.4 with virtual users (passwd-file), Maildir, IMAP, and LMTP for Postfix integration
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
    - name: Ubuntu
      versions:
        - noble
  license: Apache-2.0
allow_duplicates: false

dependencies: []
