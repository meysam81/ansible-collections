---
- name: Reload squid
  ansible.builtin.command:
    cmd: squid -k reconfigure
  changed_when: true

- name: Restart squid
  ansible.builtin.systemd:
    name: squid
    state: restarted
    daemon_reload: true
