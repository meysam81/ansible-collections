---
galaxy_info:
  author: Meysam Azad
  role_name: haproxy_base
  description: HAProxy shared infrastructure (user, dirs, error pages, systemd service)
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
    - name: Ubuntu
      versions:
        - jammy
        - noble
  license: Apache-2.0
allow_duplicates: false

dependencies:
  - role: haproxy_build
